import React from "react";

function NavBar() {
    return (
        <nav className="nav">
            <span className="nav__item--active">Random Joke</span>
        </nav>
    );
}

export default NavBar;
